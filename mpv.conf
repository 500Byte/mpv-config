# ==============================================================================
# MPV Configuration - Clean & Modern (Wayland / AMD / PipeWire)
# ==============================================================================

# --- Video ---
vo=gpu
gpu-api=vulkan
gpu-context=waylandvk
hwdec=auto-safe
dither-depth=auto
dither=ordered

# --- Audio ---
ao=pipewire
audio-channels=stereo
audio-file-auto=fuzzy
alang=en,eng,es,spa
volume=100
volume-max=150

# --- Subtitles ---
sub-auto=fuzzy
slang=es,spa,en,eng
sub-file-paths=sub;subs;subtitles
sub-font="Noto Sans"
sub-font-size=40
sub-bold=no
sub-color="#FFFFFF"
sub-outline-size=1.5
sub-outline-color="#000000"
sub-shadow-offset=0
sub-back-color="#000000"

# --- UI & Window ---
border=no
keepaspect=yes
keepaspect-window=no
geometry=1792x1008
osc=no             # Disabled for uosc
osd-bar=no         # Disabled for uosc
osd-font="Noto Sans"

# --- Playback ---
idle=yes
keep-open=yes
save-position-on-quit=yes
watch-later-options=start,edition,volume,aid,vid,sid,secondary-sid
loop-playlist=inf
autocreate-playlist=same
hr-seek=yes

# --- Screenshots ---
screenshot-format=jpg
screenshot-jpeg-quality=100
screenshot-template="~/Pictures/Screenshots/mpv-shot-%{media-title}-%P-%n"

# --- Cache & Paths ---
watch-later-dir="~~/cache/watch_later"
gpu-shader-cache-dir="~~/cache/shader"
icc-cache-dir="~~/cache/icc"
log-file="~~/cache/mpv.log"
