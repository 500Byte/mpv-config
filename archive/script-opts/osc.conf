### Configuration does not support comments after parameters, must start on a new line
### No extra spaces allowed between options and values

showwindowed=yes
# -- Show OSC when windowed, default: yes
showfullscreen=yes
# -- Show OSC when fullscreen, default: yes
#scalewindowed=1
# -- Scaling factor of OSC when windowed, default: 1
#scalefullscreen=1
# -- Scaling factor of OSC when fullscreen, default: 1
#scaleforcedwindow=2
# -- Scaling factor of OSC when rendered on a forced (dummy) window, default: 2
#vidscale=no
# -- Scale OSC with video size? Default: yes
valign=0.8
# -- <-1(top)--1(bottom)> Vertical alignment, default: 0.8
halign=0
# -- <-1(left)--1(right)> Horizontal alignment, default: 0
barmargin=0
# -- Vertical margin of bottom/top bar when using "bottombar/topbar" layout, default: 0
boxalpha=80
# -- Transparency of box element, default: 80

hidetimeout=500
# -- Duration to hide OSC if the mouse doesn't move (milliseconds). Non-negative number, default: 500
fadeduration=200
# -- Fade out duration (milliseconds), default: 200. 0 for no fade animation
deadzonesize=1
# -- Control the interaction logic between cursor and OSC area. Range from 0.0 to 1.0, default: 0.5
minmousemove=0
# -- Minimum number of pixels the mouse must move between ticks to display OSC, default: 0
iamaprogrammer=no
# -- Use native mpv values and disable OSC internal playlist management (and certain features dependent on it), default: no

layout=bottombar
# -- <box|slimbox|default bottombar|topbar> Choose built-in layout scheme
seekbarstyle=knob
# -- <default bar|diamond|knob> Choose progress bar style
seekbarhandlesize=0.6
# -- Size of diamond and knob (relative to the full height of seekbar)
seekrangestyle=inverted
# -- <bar|line|slider|default inverted|none> Style of the search range (slider is invalid when --seekbarstyle=bar)
seekrangeseparate=yes
# -- [Effective when --seekbarstyle=bar] Whether to separate seekrange from the progress bar, default: yes
seekrangealpha=200
# -- Transparency of seekrange, default: 200
seekbarkeyframes=yes
# -- Control the search mode when dragging the progress bar. If set to yes, use the default search mode (usually keyframes but depends on MPV's priority settings). If set to no, use precise search (slow). Default: yes

title=${?pause==yes:⏸}${?mute==yes:🔇}${?ontop==yes:📌}${?demuxer-via-network==yes:${media-title}}${?demuxer-via-network==no:${filename}} ${!playlist-count==1:List:${playlist-pos-1}/${playlist-count}} ${!chapters==0:Chapter:${chapter}} ${?percent-pos:Progress:${percent-pos}%}
# -- A string compatible with property expansion, displayed as OSC title (Note: If --no-boder is enabled in mpv.conf, this will also be displayed as the title without borders)
# -- Main title display content for all layouts, compatible with properties (https://mpv.io/manual/master/#property-list) and strings (https://mpv.io/manual/master/#property-expansion)
## This item also affects --no-boder, which is the upper title in no-border mode. OSC is essentially an ASS subtitle, the more variables monitored, the slower the response. The most intuitive manifestation is the change in feedback perception when moving the mouse to view the timeline

tooltipborder=1
# -- Border of tooltip in "bottombar/topbar" layout
timetotal=yes
# -- Display total time instead of remaining time, default: no
timems=no
# -- Display timecode with millisecond precision, default: no
visibility=auto
# -- <never|default auto|always> OSC visibility

boxmaxchars=150
# -- Maximum number of characters for the title under the "box" layout, default: 80
boxvideo=no
# -- Layout between OSC and video, see https://mpv.io/manual/master/#on-screen-controller-boxvideo for details
windowcontrols=auto
windowcontrols_alignment=right
# -- <left|default right> These two items are related to window controls in no-border mode (https://mpv.io/manual/master/#on-screen-controller-windowcontrols)
greenandgrumpy=no
# -- Whether to disable holiday eggs, default: no
livemarkers=yes
# -- Instantly update chapter markers, default: yes
#chapters_osd=no
# -- Display OSD all chapter information when switching chapters with left-click, default: yes
playlist_osd=no
# -- Display OSD playlist information when switching files with left-click, default: yes
#chapter_fmt=Chapter: %s
# -- Format of chapter information displayed when hovering over the progress bar, special value no for not displaying, example is the default value